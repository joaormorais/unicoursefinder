services:
  backend:
    container_name: backend_omeucurso
    restart: always
    build:
      context: ./backend
      dockerfile: Dockerfile
    networks:
      - core_network
    environment:
      - SPRING_DATASOURCE_URL=${SPRING_DATASOURCE_URL}
      - SPRING_DATASOURCE_USERNAME=${POSTGRES_USER}
      - SPRING_DATASOURCE_PASSWORD=${POSTGRES_PASSWORD}
      - FRONTEND_ORIGIN=${FRONTEND_ORIGIN}
      - SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_ISSUER_URI=${ISSUER_URI}
      - SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_JWK_SET_URI=${JWK_SET_URI}
      - KEYCLOAK_SERVER_URL=${KEYCLOAK_SERVER_URL}
      - KEYCLOAK_REALM=${KEYCLOAK_REALM}
      - KEYCLOAK_CLIENT_ID=${KEYCLOAK_CLIENT_ID}
      - KEYCLOAK_BACKEND_SERVICE_SECRET=${KEYCLOAK_BACKEND_SERVICE_SECRET}
    labels:
      - traefik.enable=true
      - traefik.docker.network=core_network
      # Path-based routing
      - traefik.http.routers.omeucurso-backend.rule=Host(`omeucurso.pt`) && PathPrefix(`/api`)
      - traefik.http.routers.omeucurso-backend.entrypoints=websecure
      - traefik.http.routers.omeucurso-backend.tls.certresolver=le
      - traefik.http.services.omeucurso-backend.loadbalancer.server.port=8080

  frontend:
    container_name: frontend_omeucurso
    restart: always
    build:
      context: ./frontend
      dockerfile: Dockerfile.prod
    networks:
      - core_network
    depends_on:
      - backend
    labels:
      - traefik.enable=true
      - traefik.docker.network=core_network
      # Path-based routing
      - traefik.http.routers.omeucurso-frontend.rule=Host(`omeucurso.pt`)
      - traefik.http.routers.omeucurso-frontend.entrypoints=websecure
      - traefik.http.routers.omeucurso-frontend.tls.certresolver=le
      - traefik.http.services.omeucurso-frontend.loadbalancer.server.port=80
      # Redirect
      - traefik.http.middlewares.www-to-non-www.redirectregex.regex=^https://www.omeucurso.pt/(.*)
      - traefik.http.middlewares.www-to-non-www.redirectregex.replacement=https://omeucurso.pt/$${1}
      - traefik.http.middlewares.www-to-non-www.redirectregex.permanent=true
      # Router www
      - traefik.http.routers.omeucurso-www.rule=Host(`www.omeucurso.pt`)
      - traefik.http.routers.omeucurso-www.entrypoints=websecure
      - traefik.http.routers.omeucurso-www.tls.certresolver=le
      - traefik.http.routers.omeucurso-www.middlewares=www-to-non-www
      - traefik.http.routers.omeucurso-www.service=omeucurso-frontend

networks:
  core_network:
    external: true